% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{woven_plot_loadings}
\alias{woven_plot_loadings}
\title{Plot feature loadings for one WOVEN latent dimension}
\usage{
woven_plot_loadings(
  fit,
  dim = 1L,
  n_top = 15L,
  feature_names = NULL,
  modality = NULL,
  main = NULL
)
}
\arguments{
\item{fit}{woven object from [woven()]}

\item{dim}{integer: which latent dimension to plot (1..K, default 1)}

\item{n_top}{integer: number of top features per modality (default 15)}

\item{feature_names}{optional list of V character vectors (one per modality).
If a plain character vector is passed for a single-modality call, it is
used for that modality. If NULL, uses rownames of W or "Feature_j".}

\item{modality}{integer or NULL: if specified, plot only that modality.
If NULL (default), all V modalities are shown in faceted panels.}

\item{main}{character: plot title. If NULL, a default is used.}
}
\value{
a ggplot object (printed automatically; add layers with \code{+})
}
\description{
For a given latent dimension, shows the top features by absolute loading
for each modality (or a selected subset), colored by loading sign.
Positive loadings are blue; negative loadings are red-orange.
Equivalent to DIABLO's \code{plotLoadings()}.
}
\examples{
set.seed(1)
n <- 60; p1 <- 30; p2 <- 20; K <- 3
Y <- rep(1:3, each = n / 3)
X1 <- matrix(rnorm(n * p1), n, p1)
X2 <- matrix(rnorm(n * p2), n, p2)
miss <- matrix(runif(n * 2) < 0.3, n, 2)
for (i in which(rowSums(miss) == 2)) miss[i, sample(2, 1)] <- FALSE
X1[miss[, 1], ] <- NA
X2[miss[, 2], ] <- NA
fit <- woven(list(X1, X2), Y = Y, K = K)
woven_plot_loadings(fit, dim = 1L)
}
\seealso{
[woven_plot_vip()], [woven_plot_variance()]
}
