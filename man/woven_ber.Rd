% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{woven_ber}
\alias{woven_ber}
\title{Balanced Error Rate via k-fold LDA cross-validation}
\usage{
woven_ber(Z, labels, n_folds = 5L)
}
\arguments{
\item{Z}{numeric matrix n x K (consensus latent scores)}

\item{labels}{integer or factor of length n (class labels)}

\item{n_folds}{integer, CV folds (default 5; use n for LOO)}
}
\value{
scalar in [0, 1], lower is better
}
\description{
BER = (1/C) * sum_c (1 - sensitivity_c)  where C = number of classes.
Uses the same LDA classifier for all methods (WOVEN, DIABLO, MOFA2) so the
comparison is fair: DIABLO's built-in sparse classifier is NOT used.
Falls back to nearest-centroid if MASS::lda is unavailable.
}
\details{
Lower is better. 0 = perfect, 1 - 1/C = chance level.
}
