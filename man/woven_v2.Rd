% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solver_v2.R
\name{woven_v2}
\alias{woven_v2}
\title{Fit supervised WOVEN with two modalities (closed-form)}
\usage{
woven_v2(
  X1,
  X2,
  anchor_idx,
  Y,
  K = 5L,
  lambda1 = 0.1,
  lambda2 = 0.1,
  gamma_y = 1,
  alpha1 = 0,
  alpha2 = 0,
  k_nn = 15L,
  sigma1 = NULL,
  sigma2 = NULL,
  X1_full = NULL,
  X2_full = NULL,
  Omega1_precomp = NULL,
  Omega2_precomp = NULL,
  L1_precomp = NULL,
  L2_precomp = NULL,
  XtX1_precomp = NULL,
  XtX2_precomp = NULL,
  verbose = FALSE
)
}
\arguments{
\item{X1}{n x p1 matrix  -- modality 1 (NA rows = block-missing)}

\item{X2}{n x p2 matrix  -- modality 2 (NA rows = block-missing)}

\item{anchor_idx}{integer vector  -- fully-observed subject indices}

\item{Y}{vector of length n  -- class labels (required; only anchors used in fit)}

\item{K}{integer  -- number of latent dimensions}

\item{lambda1, lambda2}{numeric >= 0  -- Laplacian regularization per modality}

\item{gamma_y}{numeric >= 0  -- label supervision strength (0 = unsupervised)}

\item{alpha1, alpha2}{numeric >= 0  -- L1 soft-threshold for sparse W1/W2
(0 = dense; try 0.01-0.1 * max(abs(P)) for meaningful sparsity)}

\item{k_nn}{integer  -- k-NN for Laplacian}

\item{sigma1, sigma2}{numeric or NULL  -- RBF bandwidth (NULL = median heuristic)}

\item{X1_full, X2_full}{optional unmasked matrices for Laplacian construction}

\item{Omega1_precomp, Omega2_precomp}{optional precomputed Omega (graph smoothing) matrices}

\item{L1_precomp, L2_precomp}{optional precomputed sample Laplacians}

\item{XtX1_precomp, XtX2_precomp}{optional precomputed X^T X matrices}

\item{verbose}{logical, print progress messages (default FALSE)}
}
\value{
list: W1, W2, Z1, Z2, Za1, Za2, Xa1, Xa2, col_ok1, col_ok2,
  singular_values, B1, B2, L1, L2, anchor_idx, K, and fit metadata
}
\description{
Fit supervised WOVEN with two modalities (closed-form)
}
\examples{
set.seed(1)
n <- 20
K <- 2L
X1 <- matrix(rnorm(n * 5), n, 5)
X2 <- matrix(rnorm(n * 4), n, 4)
Y <- rep(1:2, each = n / 2)
miss <- matrix(FALSE, n, 2)
miss[c(15, 17, 19), 1] <- TRUE
miss[c(16, 18, 20), 2] <- TRUE
X1[miss[, 1], ] <- NA
X2[miss[, 2], ] <- NA
anchor_idx <- which(rowSums(miss) == 0)
fit <- woven_v2(X1, X2, anchor_idx = anchor_idx, Y = Y, K = K)
dim(fit$W1)
}
