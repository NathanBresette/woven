% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{mat_sqrt}
\alias{mat_sqrt}
\title{Symmetric matrix square root via eigendecomposition}
\usage{
mat_sqrt(B, tol = 1e-10, n_rank = NULL)
}
\arguments{
\item{B}{symmetric PD matrix (p x p)}

\item{tol}{relative eigenvalue floor}

\item{n_rank}{known upper bound on rank of B (e.g. number of samples).
If provided and p > n_rank, uses truncated eig with k = min(n_rank, p-1).}
}
\value{
list with $sqrt and $inv_sqrt (both p x p)
}
\description{
Returns B^\{1/2\} and B^\{-1/2\} for a symmetric PD matrix.
When p > rank_thresh, uses truncated eigendecomposition via RSpectra  --
B = X^T M X has rank at most n, so only n eigenvectors are non-trivial.
}
\keyword{internal}
