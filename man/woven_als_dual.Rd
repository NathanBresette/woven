% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solver_als_dual.R
\name{woven_als_dual}
\alias{woven_als_dual}
\title{Fit supervised WOVEN V>=2 via exact dual-space ALS}
\usage{
woven_als_dual(
  X_list,
  anchor_idx,
  Y,
  K = 5L,
  lambdas = NULL,
  gamma_y = 1,
  k_nn = 10L,
  max_iter = 200L,
  tol = 1e-06,
  n_restarts = 5L,
  L_list_precomp = NULL,
  La_list_precomp = NULL,
  verbose = TRUE
)
}
\arguments{
\item{X_list}{list of V matrices, each n x p_v (NA rows = block-missing)}

\item{anchor_idx}{integer vector of fully-observed subject indices}

\item{Y}{vector of length n -- class labels (required)}

\item{K}{integer, number of latent dimensions}

\item{lambdas}{numeric vector length V, Laplacian regularization}

\item{gamma_y}{numeric >= 0, label supervision strength}

\item{k_nn}{integer, k-NN for Laplacian (ignored if L_list_precomp provided)}

\item{max_iter}{integer, max ALS iterations}

\item{tol}{numeric, relative convergence threshold}

\item{n_restarts}{integer, random restarts; best objective wins}

\item{L_list_precomp}{optional precomputed sample Laplacians (strongly recommended)}

\item{La_list_precomp}{optional precomputed anchor submatrices of Laplacians}

\item{verbose}{logical}
}
\value{
list compatible with woven_als: W_list, Z_list, Za_list, Xa_list,
  col_ok_list, B_list, L_list, anchor_idx, K, V, objective, obj_trace, etc.
}
\description{
Fit supervised WOVEN V>=2 via exact dual-space ALS
}
\examples{
set.seed(1)
n <- 20
K <- 2L
X1 <- matrix(rnorm(n * 5), n, 5)
X2 <- matrix(rnorm(n * 4), n, 4)
X3 <- matrix(rnorm(n * 3), n, 3)
Y <- rep(1:2, each = n / 2)
anchor_idx <- seq_len(14L)
fit <- woven_als_dual(list(X1, X2, X3), anchor_idx = anchor_idx, Y = Y, K = K)
length(fit$W_list)
}
