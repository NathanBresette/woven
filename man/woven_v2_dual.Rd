% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solver_v2_dual.R
\name{woven_v2_dual}
\alias{woven_v2_dual}
\title{Fit supervised WOVEN with two modalities (dual closed-form)}
\usage{
woven_v2_dual(
  X1,
  X2,
  anchor_idx,
  Y,
  K = 5L,
  lambda1 = 0.1,
  lambda2 = 0.1,
  gamma_y = 1,
  k_nn = 10L,
  sigma1 = NULL,
  sigma2 = NULL,
  L1_precomp = NULL,
  L2_precomp = NULL,
  La1_precomp = NULL,
  La2_precomp = NULL,
  verbose = FALSE
)
}
\arguments{
\item{X1}{n x p1 matrix -- modality 1 (NA rows = block-missing)}

\item{X2}{n x p2 matrix -- modality 2 (NA rows = block-missing)}

\item{anchor_idx}{integer vector -- fully-observed subject indices}

\item{Y}{vector of length n -- class labels (required)}

\item{K}{integer -- number of latent dimensions}

\item{lambda1, lambda2}{numeric >= 0 -- Laplacian regularization}

\item{gamma_y}{numeric >= 0 -- label supervision strength}

\item{k_nn}{integer -- k-NN for Laplacian (ignored if L1/L2 precomp provided)}

\item{sigma1, sigma2}{numeric or NULL -- RBF bandwidth (NULL = median heuristic)}

\item{L1_precomp, L2_precomp}{optional precomputed sample Laplacians}

\item{La1_precomp, La2_precomp}{optional precomputed anchor submatrices of Laplacians}

\item{verbose}{logical, print progress messages (default FALSE)}
}
\value{
list with same fields as woven_v2: W1, W2, Z1, Z2, Za1, Za2,
  Xa1, Xa2, col_ok1, col_ok2, singular_values, anchor_idx, K, p1, p2, etc.
}
\description{
Exact dual of woven_v2: all matrix operations are n_a x n_a instead of p x p.
Requires L_list_precomp (sample Laplacians); anchor-only B computed internally.
}
\examples{
set.seed(1)
n <- 20
K <- 2L
X1 <- matrix(rnorm(n * 5), n, 5)
X2 <- matrix(rnorm(n * 4), n, 4)
Y <- rep(1:2, each = n / 2)
miss <- matrix(FALSE, n, 2)
miss[c(15, 17, 19), 1] <- TRUE
miss[c(16, 18, 20), 2] <- TRUE
X1[miss[, 1], ] <- NA
X2[miss[, 2], ] <- NA
anchor_idx <- which(rowSums(miss) == 0)
fit <- woven_v2_dual(X1, X2, anchor_idx = anchor_idx, Y = Y, K = K)
dim(fit$W1)
}
