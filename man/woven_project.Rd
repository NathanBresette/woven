% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{woven_project}
\alias{woven_project}
\title{Project all samples into the shared latent space}
\usage{
woven_project(fit, X1, X2, sigma_proj = NULL)
}
\arguments{
\item{fit}{output of woven_v2}

\item{X1}{full modality 1 matrix (n x p1); NA rows = block-missing subject}

\item{X2}{full modality 2 matrix (n x p2); NA rows = block-missing subject}

\item{sigma_proj}{RBF bandwidth for projection kernel (NULL = median heuristic from anchors)}
}
\value{
list:
  $Z1     n x K latent matrix (modality 1 perspective)
  $Z2     n x K latent matrix (modality 2 perspective)
  $Z      n x K consensus embedding (feature-count-weighted average)
  $method per-subject character: "direct", "partial", or "missing"
}
\description{
Project all samples into the shared latent space
}
\examples{
set.seed(1)
n <- 20
K <- 2L
X1 <- matrix(rnorm(n * 5), n, 5)
X2 <- matrix(rnorm(n * 4), n, 4)
Y <- rep(1:2, each = n / 2)
miss <- matrix(FALSE, n, 2)
miss[c(15, 17, 19), 1] <- TRUE
miss[c(16, 18, 20), 2] <- TRUE
X1[miss[, 1], ] <- NA
X2[miss[, 2], ] <- NA
anchor_idx <- which(rowSums(miss) == 0)
fit <- woven_v2(X1, X2, anchor_idx = anchor_idx, Y = Y, K = K)
proj <- woven_project(fit, X1, X2)
dim(proj$Z)
}
